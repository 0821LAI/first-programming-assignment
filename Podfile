# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'first assignment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for first assignment
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  
  target 'first assignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'first assignmentUITests' do
    # Pods for testing
  end
end
